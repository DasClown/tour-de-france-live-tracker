[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    e2e: End-to-End-Tests gegen den laufenden Server (brauchen aktiven Dienst)
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning
